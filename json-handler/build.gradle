apply plugin: "application"

version = '1.0'

mainClassName = "org.hermes.core.App"

jar {
    manifest.attributes("Main-Class": mainClassName);
}

dependencies {
    implementation project(":core")
}